NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Used to build OAuth + email-confirmation redirect URLs in production.
# Locally the auth flow falls back to window.location.origin.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
